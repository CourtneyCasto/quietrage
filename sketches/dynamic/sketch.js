function setup() {
  // Create the canvas
  createCanvas(600, 600);
  // H, S & B integer values
  colorMode(HSB);
  background(0, 85, 90);

  // H, S & B integer values
  colorMode(HSB);
  fill(53, 42, 90);
  ellipse(300, 250, 400, 450);

  fill(0, 0, 100)
  ellipse(250, 200, 60, 80)

  fill(0, 0, 100)
  ellipse(350, 200, 60, 80)

  fill(255, 204, 100);
  ellipse(250, 200, 40, 50);

  fill(255, 204, 100);
  ellipse(350, 200, 40, 50);

  fill(102);
  rect(200, 280, 200, 75);

  line(200, 320, 400, 320)
}
